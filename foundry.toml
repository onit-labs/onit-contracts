[profile.default]
src = 'src'
out = 'out'
libs = ['lib', 'node_modules']
test = 'test'
cache_path  = 'forge-cache'
solc = '0.8.15'
gas_reports = ["*"]
optimize = true
#optimize_runs = 1
via_ir = false

[rpc_endpoints]
fuji = "${FUJI_RPC_URL}"

[etherscan]
fuji = { key = "${SNOWTRACE_API_KEY}" }